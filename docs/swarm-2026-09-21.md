# R6 canonical delivery report — 21/09/2026

Report checkpoint: 09:54 BRT, desktop. **Partial delivery, not all five demos done.**
Generator: **OpenAI / GPT-6 Astra**, recovery after verified full-GLM queue/HTTP503
failure. Prior GLM work was read-only. No reviewer spawned; coordinator owns the
full GLM independent gate, PR publication and all merges/deployments. Nothing
was merged, deployed, posted publicly or sent through private accounts.

## Scope, hypothesis and evidence

Hypothesis: marketing output was not emitted by the current implementation.
Confirmed: Kokoro client blob `eb88c8745356b31de1c7f22d6c696604b9df2087` prints
JSON on ping, nothing on successful speak/interrupt; the old cast invented
colored 412/458 ms messages. Queue acceptance is not speech onset. Chrome's GIF
was documented as rendered tokens while advertised as screen capture; its
actual debug-click returns `{ok:true}`, not a DOM `isTrusted` observation.
The Mac illustration used nonexistent CLI syntax and unlinked timings.
Fand's cast has no accompanying host/build/measurement receipt.

## Branches and disposition

All paths below are sibling feature worktrees under
`/home/notroot/Documents/Code/yolo-labz/`; parent clones were not edited.

| Repo/worktree | Base | Candidate head/state | Delivered / unfulfilled |
|---|---|---|---|
| kokoro-speakd-039-truth-media-swarm | 94cb7d2 | branch `039-truth-media-swarm`; report commit HEAD | Actual audio/video/cast, recorder, two regression checks, corrected README/PyPI wording. |
| zellij-vertical-tabs-092-media-swarm | 20416c8 | staged, uncommitted after commit-hook timeout | Exact plugin build succeeds; isolated capture fails layout validation. **No valid sidebar demo.** |
| chrome-bridge-092-media-swarm | 8a7592d | 2dfaf93 | README/CLAUDE truth correction, source evidence and isolated capture procedure. **No actual browser capture.** |
| claude-mac-chrome-092-media-swarm | 917ba77 | 2b66023 | Corrected 1200×620 illustration, fixture catalog output, capture procedure. **No AppleScript recording.** |
| fand-092-media-swarm | 9b72d0a | b5642d0 | Legacy provenance warning and read-only Mac capture plan. **No fresh telemetry.** |

PR state: none created/pushed in this slice; local branches only. Resolve exact
primary head with `git rev-parse 039-truth-media-swarm`; the report travels in that
commit. No landed work to revert. Future single-slice reversal: `git revert
<eventual-squash-commit>` on a new feature branch, then PR.

## Actual Kokoro deliverables and executed checks

- `scripts/record-demo.py`: private temporary socket/cache, private PulseAudio null
  sink, real daemon/client and monitor audio; cleanup only owned processes/module.
- `docs/assets/kokoro-demo.cast`: replacement actual client output, 100×24 cells.
- `docs/assets/recording-2026-09-21/kokoro-demo.wav`: **44.62 s**, PCM16,
  24 kHz mono; includes synthesis wait, roughly four seconds of actual speech,
  then silence after interrupt. The full generated utterance is **21.3 s** and
  separately preserved in `full-utterance.wav`, proving early interruption.
- `kokoro-demo.mp4` in that same directory: **960×240, 6.2 s, H.264/AAC mono**,
  actual playback waveform. Only initial synthesis wait trimmed, unchanged speed;
  trim at **38.47408333333333 s** in `clip-edit.json`. No music or microphone.
- Exact executed recorder text, source/base/runtime metadata and SHA-256 manifest
  accompany the media. Model revision/hashes: `docs/evidence/model-provenance.json`.
  Runtime reused the completed `/tmp/kokoro-daemonEnv` build; build log retained.
- `uv run --no-project --with pytest python -m pytest -q`: **16 passed**.
- `ruff check .`, `ruff format --check .`: **pass**.
- `uvx pyright`: **0 errors, 0 warnings**.
- `ffprobe`: PCM mono audio and H.264/AAC clip confirmed; JSON receipts in
  `docs/evidence/{audio,video}-ffprobe.json`.

Tests assert JSON/silent client behavior on an isolated socket, real nonzero
captured sound, early cutoff relative to full source length, and a quiet final
second. Initial recorder buffering dropped the quiet tail; the test failed,
then passed after explicit 20 ms recorder buffering. Failed-attempt evidence is
retained separately. No latency benchmark is claimed, and no physical sink was
used. PyPI JSON confirms published **0.3.0**; metadata lacks the Kokoro runtime
imports, so README recommends the tested Nix runtime rather than claiming bare
pip installation is complete.

Reproduction commands and license/provenance: `docs/assets/README.md`.
Changed primary files: README, replacement cast, assets/evidence, recorder,
`tests/test_demo.py`, `specs/039-truth-media/plan.md`, this report.

## Remaining evidence and gates

Zellij `nix build --no-link --print-out-paths` produced
`/nix/store/m4a24pmhzgsk3w8sd2ic21386r1iqzgv-zellij-vertical-tabs-0.1.0`;
installed runtime **0.44.3**. It is not unavailable. Attempts used fresh HOME/XDG
paths and a short private runtime path; requested layouts and raw casts are in
that worktree's `docs/evidence/`. Final `query-tab-names` returned only `Tab #1`,
so the harness rejected it. Continue layout-selection diagnosis; do not publish
those failure casts as a plugin demonstration. The code-slop commit hook exceeded
the bounded command window; no hook bypass was used, work remains staged.

Chrome bridge: `python cli/cb --help` and `node --check extension/service-worker.js`
passed; source/README assertions passed. Old GIF is 800×480, 16.2 s, no audio,
now explicitly called illustration. Fixed relay port/profile reuse prevents
claiming its normal launcher is an isolated test. Disposable OS/display procedure
is in its `docs/MEDIA.md`; no browser/account action was attempted.

Mac Chrome: actual `catalog` against `docs/fixtures/local-profile/Local State`
returned synthetic Demo profile JSON (jq assertions passed); SVG XML/label checks
passed. This does not validate AppleScript. Fand: docs-only source/provenance
inspection; no SMC writes/reloads or fan daemon changes. Both genuine recordings
need suitable isolated macOS environments and remain gated.

Secondary commits emitted existing lefthook hook-sync warnings (pre-existing
`.old` hook paths); the three listed commits exist. No hooks were disabled.
Independent review, publishing/PRs and all merges remain with the coordinator.

## Coordinator cleanup correction — 21/09/2026

An AST-extracted execution of the original finalizer with mock processes proved
that recorder wait timeout skipped daemon termination and module unload. No real
audio or process was touched. The helper now attempts every owned cleanup,
escalates terminate to kill with bounded waits, handles SIGTERM through finally,
and uses the private temporary-directory nonce in its sink name. Three mock-only
regressions cover wait timeout, failed reap and interrupt timeout.

`ruff check`, Pyright (zero errors/warnings), and all **19 tests** pass. Recorded
media/snapshot bytes are unchanged; assets README documents recorder drift and
SIGKILL limits. Earlier GLM requirements/security PASS verdicts bind to 9e8e68e,
not this changed head; renewed exact-head review is required.
